# Briefs Registry

This directory holds the production briefs for proof-first video masters.
Each brief is the written contract that connects a real Summa workflow to the
captures, shots, and template needed to produce a reliable upstream master.

## What a brief must define

- feature domain
- real user problem
- target audience
- promise or expected outcome
- approved captures needed to prove the workflow
- target master format
- later derived channels, if any

## How to use this registry

- Treat each brief as the source of truth for one workflow.
- Use real Summa UI proof when the scene is meant to demonstrate product value.
- Do not invent captures inside the brief; captures must be listed in the
  capture registry first.
- Keep the brief narrow enough that a future product-film editor can build from
  it without re-litigating the product story.

## Brief structure

Each brief file should stay readable on its own and follow the same shape:

- title and brief id
- workflow summary
- user problem
- audience
- product promise
- proof requirements
- master format
- derived outputs
- notes and constraints

## Current briefs

- `importacio-extracte-conciliacio.md`

## Current rule

The first brief in this registry must be a concrete workflow that can be proven
with captures. For this branch, that workflow is bank statement import and
reconciliation.
