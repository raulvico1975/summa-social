# Functional Explainer Brief Template

## Role

Reusable brief template for proof-backed Summa Social explainers.

Use this template when the goal is a proof-first master with one proof target
per beat.

## How to use

- Fill this brief before building any proof-first master.
- Keep the scope narrow enough that every claim can be proved with a real capture.
- Use the shot-language and product-film contracts only after this brief is approved.
- Do not add ornamental story beats that do not map to product proof.

## Brief id

`<brief-id>`

## Title

`<brief-title>`

## Feature domain

`<feature-domain>`

## Real user problem

Describe the operational problem in one short paragraph.

`<user-problem>`

## Audience

- `<primary-audience>`
- `<secondary-audience>`

## Product promise

State the single outcome the viewer should believe after watching.

`<promise>`

## Explainer angle

Describe the functional angle in one sentence.

`<angle>`

## What the piece must prove

List the claims the video is allowed to make.

- `<claim-1>`
- `<claim-2>`
- `<claim-3>`
- `<claim-4>`

Rules:

- one claim per beat
- each claim must be testable against a real capture
- do not mix proof and narration in the same line

## Proof requirements

List the exact UI states or product moments that can prove the claims.

- `<proof-target-1>`
- `<proof-target-2>`
- `<proof-target-3>`
- `<proof-target-4>`

Rules:

- one proof target per beat
- no abstract proof
- no illustrative stand-ins if a real capture exists

## Proof sequence

Use this table to lock the claim-to-proof relationship before any premium edit.

| Beat | Job | Claim | Proof target | Required capture | Film note |
| --- | --- | --- | --- | --- | --- |
| 1 | `<job-1>` | `<claim-1>` | `<proof-target-1>` | `<capture-1>` | `<film-note-1>` |
| 2 | `<job-2>` | `<claim-2>` | `<proof-target-2>` | `<capture-2>` | `<film-note-2>` |
| 3 | `<job-3>` | `<claim-3>` | `<proof-target-3>` | `<capture-3>` | `<film-note-3>` |
| 4 | `<job-4>` | `<claim-4>` | `<proof-target-4>` | `<capture-4>` | `<film-note-4>` |

## Approved captures needed

List the real product captures required to prove the beat matrix.

- `<capture-1>`
- `<capture-2>`
- `<capture-3>`
- `<capture-4>`

Rules:

- captures must already exist or be explicitly scheduled for recording
- capture descriptions must be specific enough to identify the UI state
- the brief does not invent proof

## Target master format

`<master-format>`

## Later derived channels

- `<derived-channel-1>`
- `<derived-channel-2>`
- `<derived-channel-3>`

## Constraints

- keep the narrative product-led, not campaign-led
- keep copy sparse and operational
- keep one main idea per beat
- keep one proof target per beat
- avoid ornamental motion or generic stock-language framing
- do not widen the story beyond the approved workflow

## Notes and constraints

Use this section for any scope boundaries, terminology, or operational warnings.

`<notes>`

## Sign-off

This brief is ready when every claim has a proof target, every proof target has
a capture, and the proof sequence can move downstream without inventing a new
story.
