# `help/topics` status

Aquest directori queda congelat com a font legacy per adaptadors antics.

## Contracte actiu

No és font de veritat per a l'ajuda visible actual.

Fonts de veritat actives:

- `src/i18n/locales/*.json` per `help.*` (`HelpSheet`)
- `public/docs/manual-usuari-summa-social.{ca,es,fr}.md` per al manual runtime
- `docs/kb/cards/**/*.json` + `docs/kb/_fallbacks.json` per al bot

## Regla pràctica

- No crear-hi microfluxos nous.
- No editar-lo per ampliar la iteració 1.
- Només conservar-lo com a material congelat fins que es retiri la dependència legacy corresponent.

Referència editorial: `docs/help/HELP-EDITORIAL-SINGLE-CONTRACT.md`
